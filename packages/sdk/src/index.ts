/**
 * @pixela/playwright-reporter
 *
 * Scaffold only. The Playwright custom reporter (CI env autodetection, sharding
 * aggregation, soft mode, client-side sha256, Mode-A baseline upload) is implemented in
 * Phase 3 — see docs/spec/agents/05-playwright-reporter.md and docs/spec/playwright/*.
 */
export const PIXELA_REPORTER_PACKAGE = '@pixela/playwright-reporter';
