{
  "name": "@pixela/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types generated from the API's OpenAPI 3.1 contract (apps/api/api/openapi.yaml). The Go handler structs are the single source of truth; `gen` regenerates these — never hand-edit src/api.d.ts.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "gen": "openapi-typescript ../../apps/api/api/openapi.yaml -o src/api.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "openapi-typescript": "^7.13.0",
    "typescript": "^5.7.3"
  }
}
