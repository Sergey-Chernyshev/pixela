{
  "name": "@pixela/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types mirroring the API contract (docs/spec/specs/04-api-contract.md). Scaffold — populated from Phase 1 onward.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
